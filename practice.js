var openLock = function(deadends, target, hash = false, count = 0, current = '0000') {
    if(!hash){
    deadend.forEach((end) =>{
        hash[end] = true;
    });
    }
    
    for(let i = 0; i < current.length; i++){
        let now = parseInt(current[i]);
        let targ = parseInt(target[i]);
        if (now != target){
            
            
        }
    }
};